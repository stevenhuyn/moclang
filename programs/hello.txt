// Example con - con situation

// const net = INet.default();
// console.log("Start");
// net.display();

// const a = net.alloc(AgentKind.Con);
// const b = net.alloc(AgentKind.Con);
// console.log("allocate a b");
// net.display();

// const a_legs = net.alloc(AgentKind.Dup);
// const b_legs = net.alloc(AgentKind.Dup);

// net.link(Port.left(a), Port.left(a_legs));
// net.link(Port.right(a), Port.right(a_legs));

// net.link(Port.left(b), Port.left(b_legs));
// net.link(Port.right(b), Port.right(b_legs));

// net.link(Port.prin(a_legs), Port.prin(0));
// net.link(Port.prin(b_legs), Port.right(0));

// net.link(Port.prin(a), Port.prin(b));
// console.log("Linked");
// net.display();

// net.rewrite(a, b);

// console.log("reduce");
// net.display();
